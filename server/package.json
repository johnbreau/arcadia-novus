{
  "name": "arcadia-novus",
  "version": "1.0.0",
  "description": "web app rpg",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "john a breau",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
